#ifndef add_H					// (prevent multiple inclusion)
#define add_H

// functions in this module

dspprototypes(op_add)
dspprototypes(op_subtract)
dspprototypes(op_add_constant)
dspprototypes(op_invert)
dspprototypes(op_absolute_value)

#endif // add_H
