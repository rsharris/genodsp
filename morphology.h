#ifndef morphology_H			// (prevent multiple inclusion)
#define morphology_H

// functions in this module

dspprototypes(op_close)
dspprototypes(op_open)
dspprototypes(op_dilate)
dspprototypes(op_erode)

#endif // morphology_H
