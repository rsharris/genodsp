#ifndef sum_H					// (prevent multiple inclusion)
#define sum_H

// functions in this module

dspprototypes(op_window_sum)
dspprototypes(op_sliding_sum)
dspprototypes(op_smooth)
dspprototypes(op_cumulative_sum)

#endif // sum_H
