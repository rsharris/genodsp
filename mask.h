#ifndef mask_H					// (prevent multiple inclusion)
#define mask_H

// functions in this module

dspprototypes(op_mask)
dspprototypes(op_mask_not)
dspprototypes(op_clip)
dspprototypes(op_erase)

#endif // mask_H
